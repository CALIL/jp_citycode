[project]
name = "jp-citycode"
version = "0.1.0"
description = "総務省が提供する全国地方公共団体コードをJSON形式に変換する"
readme = "README.md"
requires-python = ">=3.14"
authors = [{ name = "Ryuuji Yoshimoto", email = "ryuuji@calil.jp" }]
dependencies = [
    "click",
    "jaconv",
]

[tool.uv]
package = false
